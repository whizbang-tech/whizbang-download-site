{
  "name": "whizbang-download-site",
  "version": "1.0.0",
  "private": true,
  "description": "Public Whizbang Mac download landing page served from the Mac Studio",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/**/*.test.mjs",
    "check": "node --check server.mjs && node --check public/app.js && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
